# 拼多多服务端研发工程师作品说明

## 项目：瑞幸咖啡 RAG 企业知识库问答系统

这是一个以 **FastAPI** 为服务层、**ChromaDB** 为向量数据库、**LangChain** 为
检索编排框架的企业知识问答服务。系统将 Markdown 企业资料切分、向量化并持久化，
通过 HTTP API 提供带来源片段的检索增强问答能力，并提供简洁 Web 页面和 Swagger API 文档。

## 服务端设计要点

- **API 服务**：FastAPI 实现健康检查、问答、向量库状态接口，并以 Pydantic 定义请求和响应结构。
- **RAG 链路**：文档加载、按标题分块、嵌入、ChromaDB 持久化检索、DeepSeek 生成回答，形成可复现的数据处理链路。
- **可配置性**：密钥、端口、模型与向量库路径均通过环境变量管理，避免把敏感信息写入代码。
- **可观测性与评估**：项目保留离线评测脚本与评测报告，便于验证检索与回答质量。
- **可移植交付**：本投递包不包含密钥、模型权重、缓存或运行日志；首次运行自动下载轻量中文嵌入模型并建立本地索引。

## 运行方式

### 环境

- Python 3.10+
- 可访问 DeepSeek API（需要自己的 API Key）
- 首次运行需要联网下载嵌入模型

### 启动

1. 将 `.env.example` 复制为 `.env`。
2. 在 `.env` 填写 `DEEPSEEK_API_KEY`。
3. 安装依赖：`pip install -r requirements.txt`。
4. 启动：`python start_server.py`。
5. 浏览器访问 `http://localhost:8001/index.html`；接口文档在 `http://localhost:8000/docs`。

首次启动会根据 `documents/` 下的资料创建向量库，后续启动会直接复用本地向量库。

## 主要接口

- `GET /`：服务健康检查
- `GET /health`：向量库状态与文档数量
- `POST /ask`：提交 `{"question": "瑞幸咖啡成立于哪一年？"}`，返回回答及引用的来源片段

## 投递包内容说明

压缩包只包含源代码、知识库样例、依赖清单、配置模板、前端和说明文档。为控制文件体积，
不包含可自动下载或自动生成的内容：本地模型、向量数据库、缓存、日志、临时文件和真实 `.env`。
这些内容均保留在原项目目录中，没有删除。
